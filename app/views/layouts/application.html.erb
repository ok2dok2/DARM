<!DOCTYPE html>
<html>
  <head>
    <title>Last</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  </head>

  <body>
  <div class="kotei">
    <% if notice %>
      <p class="alert alert-notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-error"><%= alert %></p>
    <% end %>

    <%= stylesheet_link_tag 'styles', media: 'all', 'data-turbolinks-track': 'reload' %>
      <div class="login_language">
        <% if user_signed_in? %>
          <%= link_to t("logout"), destroy_user_session_path, method: :delete %>
          <%= link_to t("mypage"), user_path(current_user.id), data: {"turbolinks" => false} %>
        <% else %>
          <%= link_to t("sign up"), new_user_registration_path %>
          <%= link_to t("login"), new_user_session_path %>
        <% end %>
        <div class= "language">
          <details>
            <summary>language</summary>
              <ul>
                <li style="list-style: none;"><%= link_to '한국어', change_locale_path(:ko), data: {turbolinks: "false"} %></li>
                <li style="list-style: none;"><%= link_to '日本語', change_locale_path(:ja), data: {turbolinks: "false"} %></li>
              </ul>
          </details>
      </div>


      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="<%= root_path %>"><%= image_tag 'logo_touka.png', class:'topimage' %><span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= topics_path %>"><br>TOPICS
              <br><i class="bi bi-pencil-square"></i></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= events_path %>"><br>EVENTS
              <br><i class="bi bi-person-hearts"></i></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= users_path %>"><br>USERS
              <br><i class="bi bi-person-circle"></i></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= conversations_path %>"><br>MESSAGES
              <br><i class="bi bi-chat-quote"></i></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= rails_admin_path %>"><br>ADMIN
              <br><i class="bi bi-tools"></i></a>
            </li>
          </ul>
        </div>
      </nav>
    </div>

  <%= yield %>
  </body>
    <footer class="footer text-center">
      <p>&copy; 2023 DARM. All rights reserved.</p>
    </footer>
</html>
