<tbody>
  <table width="80%">
    <% @user.followers.each do |user| %>
    <tr>
        <td>
          <%= link_to user_path(user.id) do%>
          <% if user.images.present? %>
          <%= image_tag user.first_image ,class: "user_icon" %>
          <% end %>
          <% end %>
        </td>
        <td><p><%= user.name %></p><p><%= user.age %>age</p></td>
        <td><%= user.introduce %></td>
        <td><%= render 'users/tag_list', tag_list: user.tag_list %></td>
        <% unless user.id == current_user.id %>
        <td><%= render 'follow_form', user: user %></td>
        <td><%= link_to conversations_path(sender_id: current_user.id, recipient_id: user.id), method: :post do %>
          <%= image_tag 'mail150.png', class:"mail_image" %></td>
          <% end %>
        <% end %>
        <td><%= link_to '編集', edit_user_registration_path%></td>
        <td><%= link_to '削除', user_registration_path, method: :delete %></td>
    <% end %>
    </tr>
    </table>
  </tbody>